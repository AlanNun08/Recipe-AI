# Frontend Environment Variables for Local Development

# Comment out REACT_APP_BACKEND_URL to use relative paths
# REACT_APP_BACKEND_URL=http://localhost:8080

# WebSocket port for development server
WDS_SOCKET_PORT=3000

# Optional: Disable HTTPS in development
HTTPS=false

# Optional: Disable browser opening automatically
BROWSER=none

# Optional: Skip preflight checks
SKIP_PREFLIGHT_CHECK=true

# Proxy configuration for development
# This tells the React dev server to proxy API calls to your backend
REACT_APP_PROXY_TARGET=http://localhost:8080